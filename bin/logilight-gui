#!/bin/sh
# LogiLight GUI launcher. Works inside the snap and from a plain checkout.
set -e
if [ -n "${SNAP:-}" ]; then
	PYTHONPATH="$SNAP/lib:$SNAP/usr/lib/python3/dist-packages${PYTHONPATH:+:$PYTHONPATH}"
	export PYTHONPATH
fi
exec "${PYTHON:-python3}" -m logilight.gui "$@"
