#!/bin/sh
# LogiLight CLI launcher. Talks to the daemon; needs no device privileges.
set -e
if [ -n "${SNAP:-}" ]; then
	PYTHONPATH="$SNAP/lib:$SNAP/usr/lib/python3/dist-packages${PYTHONPATH:+:$PYTHONPATH}"
	export PYTHONPATH
fi
exec "${PYTHON:-python3}" -m logilight.cli "$@"
